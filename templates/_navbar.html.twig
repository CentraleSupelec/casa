<nav class="navbar navbar-expand-sm navbar-light fixed-top mb-3">
    <div class="container">
        <a class="navbar-brand align-items-center"
            href="{{ path('app_home') }}">
            <img class="me-2"
                style="max-width:200px;"
                src="{{ asset('build/images/logo-UPS-CASA-rvb.png') }}"
                alt="Universite Paris-Saclay CASA" />

            <div>
                <span class="visually-hidden">
                    {{ 'home.homepage_link'|trans }}
                </span>
                <span class="d-sm-block d-lg-none mx-auto baseline">
                    {{ 'home.nav.title'|trans }}
                </span>
            </div>
        </a>
        <button class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="{{ 'menu.toggle_menu'|trans }}">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <span class="d-none d-lg-block mx-auto">
                {{ 'home.nav.title'|trans }}
            </span>
            <ul class="navbar-nav ms-auto">
                <li class="nav-item p-2 p-sm-0 me-sm-3">
                    {% set other_locale = app.request.locale != 'fr'
                        ? 'fr'
                        : 'en'
                    %}
                    <a class="text-decoration-none d-flex flex-row"
                        href="{{
                        path(
                            'app_home',
                            {
                                _locale: other_locale
                            }
                        )
                        }}">
                        <span class="border border-primary p-1 rounded-2 me-1 d-flex justify-content-center align-items-center">
                            <span class="d-flex align-items-center">
                                {{
                                    other_locale
                                        |locale_name(other_locale)
                                        |u.title
                                }}
                            </span>
                        </span>
                    </a>
                </li>

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item p-2 p-sm-0 me-sm-3">
                        <a class="text-decoration-none d-flex flex-row"
                            href="{{ path('app_student_profile') }}">
                            <span class="border border-primary p-1 rounded-2 me-1 d-flex justify-content-center align-items-center">
                                <i class="fa-solid fa-user fa-xl text-primary">

                                </i>
                            </span>
                            <span class="d-flex align-items-center">
                                {{ 'profile.title.profile'|trans }}
                            </span>
                        </a>
                    </li>
                    <li class="nav-item p-2 p-sm-0">
                        <a class="text-decoration-none d-flex flex-row"
                            href="{{ path('app_logout') }}">
                            <span class="border border-primary p-1 rounded-2 me-1 d-flex justify-content-center align-items-center">
                                <i class="fa-solid fa-arrow-right-from-bracket fa-xl text-primary">

                                </i>
                            </span>
                            <span class="d-flex align-items-center">
                                {{ 'authentication.log_out'|trans }}
                            </span>
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item p-2 p-sm-0">
                        <a class="text-decoration-none d-flex flex-row"
                            href="{{ path('app_login') }}">
                            <span class="border border-primary p-1 rounded-2 me-1 d-flex justify-content-center align-items-center">
                                <i class="fa-solid fa-arrow-right-to-bracket fa-xl text-primary">

                                </i>
                            </span>
                            <span class="d-flex align-items-center">
                                {{ 'authentication.log_in.button'|trans }}
                            </span>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
